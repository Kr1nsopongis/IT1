const styggeOrd = ["stygg", "styggere", "styggest"]
let inputOrd = ["styggere","snill"]

if(styggeOrd.indexOf(inputOrd) == -1) {
    //utføre kode
}
else{
    // du har skrevet et stygt ord
}

for (let i = 0; i < inputOrd.length; i++) {
    if(styggeOrd.indexOf(inputOrd[i]) == -1) {
        //utføre kode
    }
    else{
        // du har skrevet et stygt ord
    }
    
}